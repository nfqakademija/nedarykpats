<section class="Ad Ad-link"
         onclick="location.href = '{{ path('advert', {'id': advert.id }) }}'">
    <header class="Ad-header">
        <div> {{ advert.createdAt|date("Y-m-d") }} </div>

        <div class="Bid">
            {% if advert.getOffersCount()  > 0 %}
                <span class="Bid-header">Aplikavo</span>
                <span class="Bid-count">{{ advert.getOffersCount() }}</span>
            {% else %}
                <span class="Bid-header">&#8203;</span>
                <span class="Bid-count">&#8203;</span>
            {% endif %}
        </div>
    </header>

    <div class="Bid-header u-margin-top">
        {% if (is_granted('IS_AUTHENTICATED_FULLY') and app.user == advert.user) %}
            Šis skelbimas tavo!
        {% else %}
            &#8203;
        {% endif %}
    </div>

    <h3 class="Header">{{ advert.title }}</h3>
    <div class="Ad-body">
        <div class="Ad-text Ad-text--top">
            <p>{{ advert.text }}</p>
        </div>
        <footer class="Ad-footer u-margin-top">
            <ul class="Ad-categories">
                {% for advertCategory in advert.categories  %}
                    <li class="Category {{ advertCategory.cssStyle }} not-active">{{ advertCategory.title }}</li>
                {% endfor %}
            </ul>
        </footer>
    </div>
</section>
